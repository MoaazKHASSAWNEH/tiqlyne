{
  "name": "motion-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Framework-agnostic TypeScript motion engine for Structifyx, Sondatio and dynamic runtimes.",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  },
  "packageManager": "pnpm@10.0.0"
}